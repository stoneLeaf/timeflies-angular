import { TimerDisplayPipe } from './timer-display.pipe';

describe('TimerDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TimerDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
